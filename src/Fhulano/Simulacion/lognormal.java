package Fhulano.Simulacion;

public class lognormal {
    // public static void main(String[] args) {
    // double sem = Math.random() * 0.95;
    // double ret = 0.0;
    // double inte = 0.0;
    // int i =1;
    // double del = 6*sig/n;
    // while (inte< sem){
    // ret = i *del;
    // // inte+=pow(e,-1/2*pow((ret-n)/s,2)) * dx/math.sqrt(2*pi*pow);

    // inte+=Math.pow(-(pow(Math.log(ret)-m,2))*del/mat, b);
    // i++;
    // }
    // }

}